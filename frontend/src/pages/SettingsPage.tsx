import { EmptyState } from '../components/states/EmptyState'

export function SettingsPage() {
  return <EmptyState title="Settings placeholder" description="Auth, notifications, and platform settings will land in dedicated modules." />
}
